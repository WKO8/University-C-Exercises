#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int raio = 1;
    float area = pow((3.14 * raio), 2);

    printf("Um círculo com raio igual a %d possui a área igual a %f", raio, area);
}